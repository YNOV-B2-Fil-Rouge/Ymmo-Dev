# MariaDB (read-only analytics). Point at the same database as the Go API.
DB_HOST=127.0.0.1
DB_PORT=3306
# Read-only user dedicated to the AI service.
DB_USER=ymmo_ai
DB_PASSWORD=change-me-ai
DB_NAME=ymmo
