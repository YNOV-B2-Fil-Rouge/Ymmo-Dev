# Copy to ".env" to override docker-compose defaults.
# docker compose reads this file automatically.

# MariaDB
MARIADB_ROOT_PASSWORD=rootpass
DB_NAME=ymmo
DB_USER=ymmo_user
DB_PASSWORD=ymmo_pass

# API
APP_ENV=development
JWT_SECRET=change-me-with-a-long-random-string
